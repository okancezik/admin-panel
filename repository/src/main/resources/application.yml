spring:
  application:
    name: repository
  datasource:
    url: jdbc:postgresql://localhost:5432/admin-panel
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect